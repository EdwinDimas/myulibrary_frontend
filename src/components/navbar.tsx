import { Outlet } from "react-router"


const NavBar = () => {
    return (
        <>
        <nav>
            this is a navbar
        </nav>
        <Outlet/>
        </>
    )
}

export default NavBar